/**
 * Lists Persons routes layout
 */
import { Stack } from 'expo-router';
import React from 'react';

export default function ListsPersonsLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}
