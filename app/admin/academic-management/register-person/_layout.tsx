/**
 * Register Person routes layout
 */
import { Stack } from 'expo-router';
import React from 'react';

export default function RegisterPersonLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}
